name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    name: Build + package DMG
    runs-on: macos-15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate project
        run: xcodegen generate

      - name: Build (Release, ad-hoc signed)
        run: |
          xcodebuild \
            -project TokenEater.xcodeproj \
            -scheme TokenEaterApp \
            -configuration Release \
            -derivedDataPath build \
            -destination 'platform=macOS' \
            CODE_SIGN_IDENTITY="-" CODE_SIGN_STYLE=Manual CODE_SIGNING_REQUIRED=NO \
            build

      - name: Package DMG
        run: |
          STAGING="dmg-staging"
          mkdir -p "$STAGING"
          cp -R build/Build/Products/Release/RaiUsage.app "$STAGING/"
          ln -s /Applications "$STAGING/Applications"
          hdiutil create \
            -volname "RaiUsage" \
            -srcfolder "$STAGING" \
            -ov -format UDZO \
            "RaiUsage-${GITHUB_REF_NAME}.dmg"

      # No paid Apple Developer account, so the DMG is ad-hoc signed and NOT
      # notarized. First launch needs Privacy & Security > Open Anyway.
      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "${GITHUB_REF_NAME}" \
            "RaiUsage-${GITHUB_REF_NAME}.dmg" \
            --title "RaiUsage ${GITHUB_REF_NAME}" \
            --notes "RaiUsage ${GITHUB_REF_NAME}

          This build is ad-hoc signed and **not notarized** (no paid Apple Developer account),
          so the first launch shows *\"Apple could not verify RaiUsage is free of malware\"*.
          Click **Done** (not Move to Trash), then **System Settings > Privacy & Security >
          Open Anyway**, launch again and confirm. Once per machine. If Open Anyway doesn't
          appear (managed Macs), run \`xattr -cr /Applications/RaiUsage.app\` in Terminal."
